# GCP Configuration
GCP_PROJECT=your-project-id
GCP_ZONE=us-east1-b
GCP_REGION=us-east1
GCP_INSTANCE=mc
GCP_MACHINE_TYPE=e2-medium
GCP_BOOT_DISK_SIZE=10GB

# Minecraft Server
MINECRAFT_VERSION=1.21.11
PAPER_BUILD=69
RCON_PASSWORD=your-secure-rcon-password

# Auto-shutdown (seconds of no players before shutdown)
IDLE_TIMEOUT=60

# Duck DNS (free dynamic DNS)
# 1. Go to https://www.duckdns.org/ and sign in
# 2. Create a subdomain (e.g., "mc-kangaroos")
# 3. Copy your token and subdomain below
DUCKDNS_TOKEN=your-duckdns-token
DUCKDNS_DOMAIN=your-subdomain

# Cloud Function URL (populated after deployment)
CLOUD_FUNCTION_URL=https://REGION-PROJECT.cloudfunctions.net/mc-start
